name: Build mmore CPU & Upload to Release
on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # 🔥 无版本锁、无冲突、自动兼容安装
      - name: Install ALL dependencies (CPU only)
        run: |
          python -m pip install --upgrade pip
          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
          pip install dask distributed mammoth python-docx python-multipart python-dotenv
          pip install pandas numpy scipy click tqdm pyarrow pillow pydantic
          pip install transformers sentence-transformers faiss-cpu
          pip install langchain langchain-core langchain-community
          pip install langchain-milvus langchain-aws langchain-cohere
          pip install langchain-openai langchain-huggingface langchain-mistralai
          pip install boto3 botocore cohere dacite
          pip install -e .

      # ✅ 正确命令
      - name: Run process
        run: |
          python -m mmore process --config-file examples/process/test_config.yaml

      - name: Run index
        run: |
          python -m mmore index --config-file examples/index/config.yaml

      # ✅ 分包打包 ≤2GB
      - name: Package cache
        run: |
          tar -cvf - . | split -d -b 2000M - mmore-output.tar.

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: mmore-output.tar.*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
