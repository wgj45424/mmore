data_path: examples/sample_data/pdf
google_drive_ids: []
previous_results: null

dispatcher_config:
  output_path: examples/process/outputs/
  use_fast_processors: true  # 🔥 开启快速模式
  distributed: false
  dashboard_backend_url: null
  extract_images: false      # 🔥 禁用图片提取（大幅提速）
  scheduler_file: null

  process_batch_sizes:
    - PDFProcessor: 4000

  processor_config:
    PDFProcessor:
      - use_fast_processor: true  # 🔥 极速模式，不做重型OCR
