data_path: examples/sample_data/pdf
google_drive_ids: []
previous_results: null

dispatcher_config:
  output_path: examples/process/outputs/
  use_fast_processors: false    # 🔥 关闭快速模式 → 才会加载模型
  distributed: false
  dashboard_backend_url: null
  extract_images: false         # 不提取图片，避免卡死
  scheduler_file: null

  process_batch_sizes:
    - PDFProcessor: 100

  processor_config:
    PDFProcessor:
      - use_fast_processor: false  # 🔥 关闭快速PDF → 加载AI模型
